apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eru-labs-brand-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "eru-labs-brand-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eru-labs-brand-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
