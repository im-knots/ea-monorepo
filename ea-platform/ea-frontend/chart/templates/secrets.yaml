apiVersion: v1
kind: Secret
metadata:
  name: ea-frontend-secrets
  labels:
    app: {{ include "ea-frontend.name" . }}
type: Opaque
data:
  {{- with .Values.secrets }}
  {{- range $key, $value := . }}
  {{ $key | quote }}: {{ $value | b64enc | quote }}
  {{- end }}
  {{- end }}