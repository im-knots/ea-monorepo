apiVersion: v1
kind: Service
metadata:
  name: {{ include "ea-job-engine.fullname" . }}
  labels:
    {{- include "ea-job-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ea-job-engine.selectorLabels" . | nindent 4 }}
