apiVersion: v1
kind: Secret
metadata:
  name: ea-job-engine-secrets
  labels:
    app: {{ include "ea-job-engine.name" . }}
type: Opaque
data:
  {{- with .Values.secrets }}
  {{- range $key, $value := . }}
  {{ $key | quote }}: {{ $value | b64enc | quote }}
  {{- end }}
  {{- end }}